

## План: комплексное улучшение дизайна сайта

Реализуем все предложенные улучшения поэтапно.

---

### Шаг 1. Hero-секция (Index.tsx)

- Заменить заголовок "ALISTA" на **"Импорт авто из Японии и Кореи под ключ"** (ALISTA оставить как подзаголовок/бренд сверху)
- Добавить **3 анимированных счётчика** под описанием: `700+ авто`, `5 лет опыта`, `от 1 дня оформление` (используя существующий `AnimatedCounter`)
- Добавить **пульсирующую анимацию** на главную CTA-кнопку (через Tailwind `animate-pulse` кастомный или тень)
- Улучшить hover-эффекты кнопок (scale + shadow transition)

### Шаг 2. Секция «Почему выбирают нас» (Index.tsx)

- Обновить описания с конкретными цифрами: «Оформление от 1 дня», «700+ автомобилей оформлено», «5 лет на рынке»
- Добавить **hover scale-эффект** на карточки (`hover:scale-[1.02]` + `transition-transform`)

### Шаг 3. Блок социального доказательства — новый компонент `StatsSection.tsx`

- Секция с 4 крупными числовыми метриками в ряд (с `AnimatedCounter`): 700+ авто, 5 лет, 1200+ клиентов, от 1 дня
- Размещается между галереей и режимом работы

### Шаг 4. Финальный CTA-блок перед футером (Index.tsx)

- Новая секция «Готовы рассчитать стоимость?» с кнопками «Калькулятор» и «Связаться»
- Градиентный фон, крупный текст

### Шаг 5. Калькулятор — улучшение пустого состояния (Calculator.tsx)

- В правой панели до расчёта: показать **пример** («Например, Toyota Camry 2.5л, 3-5 лет — ~850 000 ₽»)
- Добавить подсказку-совет

### Шаг 6. Микроанимации (index.css + компоненты)

- Добавить CSS утилиту `.hover-lift` (transform scale + shadow на hover)
- Применить к кнопкам и карточкам по всему сайту

---

### Файлы для изменения

| Файл | Что делаем |
|---|---|
| `src/pages/Index.tsx` | Hero, features, stats, CTA-блок |
| `src/components/StatsSection.tsx` | Новый — счётчики метрик |
| `src/pages/Calculator.tsx` | Улучшение пустого состояния |
| `src/index.css` | Утилита `.hover-lift`, пульс-анимация |

